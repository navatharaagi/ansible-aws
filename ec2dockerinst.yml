--- # install docker,pull images and run
-  hosts: aws
   remote_user: ec2-user
   become_method: sudo
   become: true
   gather_facts: yes
   connection: ssh
   vars_files:
    - tomcat-vars.yml

   tasks:
     
